// src/components/SchedulerConfigForm.tsx
import type { SchedulerConfig } from '../domain/types';

export function SchedulerConfigForm({ config, onChange }: { config: SchedulerConfig, onChange: (c: SchedulerConfig) => void }) {
  return <div title="Config Form">Config Form Stub</div>;
}
